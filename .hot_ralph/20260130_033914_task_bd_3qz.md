I'll implement word wrap support for the syntax highlighter. Let me start by understanding the current implementation.Now let me look at how other renderables handle word wrap to understand the pattern used in this codebase.Now let me check the Segment module to understand the segment interface better.Now I have a good understanding of the codebase. Let me write a todo list for this task and implement word wrap support.Now I need to implement the core word wrap logic. I'll add a helper function that wraps styled segments while preserving their styles across wrapped lines.Now I need to update the render function to use word wrap when enabled. Let me look at the current render function implementation more carefully.Now I'll update the render function to integrate word wrap. The key approach is:
1. Highlight each line into segments
2. If word wrap is enabled and the line is too long, wrap the highlighted segments
3. This preserves syntax coloring across wrapped linesNow I need to add tests for the word wrap functionality. Let me add them after the existing tests.All tests pass. Let me run the tests specifically for the syntax module to make sure our new tests work.All tests pass. Let me also verify with optimization levels as specified in the TESTING.md.All tests pass. Now let me commit the changes.Task completed successfully. Here's a summary of the implementation:

## Summary

Implemented word wrap support for syntax-highlighted code in `src/renderables/syntax.zig`:

**Changes made:**
1. Added `word_wrap: bool` field to the `Syntax` struct
2. Added `withWordWrap()` builder method
3. Implemented `wrapSegments()` helper function that wraps styled segments while preserving their styles across wrapped lines
4. Updated `render()` to use the wrap logic when `word_wrap` is enabled, accounting for line number width
5. Added continuation indentation for wrapped lines when line numbers are displayed

**Key features:**
- Preserves syntax coloring across wrapped lines (e.g., a long string literal will stay green on all wrapped lines)
- Correctly accounts for line number prefix width when calculating available code width
- Adds continuation indent (matching line number width) for wrapped lines
- Handles Unicode characters properly using cell-based width calculation

**Tests added:**
- `Syntax.withWordWrap` - verifies the builder method
- `Syntax.render with word wrap - short line` - verifies short lines don't wrap unnecessarily
- `Syntax.render with word wrap - long line wraps` - verifies long lines wrap correctly
- `Syntax.render word wrap preserves styles` - verifies styles are maintained across wraps
- `Syntax.render word wrap with line numbers` - verifies interaction with line numbers
- `wrapSegments basic` - unit test for the wrap function
- `wrapSegments preserves style across wrap` - verifies style preservation
- `wrapSegments handles multiple segments` - verifies handling of multiple styled segments
- `wrapSegments zero width returns input` - edge case handling