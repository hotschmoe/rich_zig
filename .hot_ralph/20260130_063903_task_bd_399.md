I'll implement the ability to highlight specific line numbers in the syntax highlighter. Let me first examine the current syntax.zig implementation.Now I understand the current implementation. I need to add the ability to highlight specific line numbers. This is useful for showing error locations, breakpoints, or other marked lines. Let me implement this feature.Now let me add the `highlight_lines` field and builder methods to the Syntax struct.Now add the builder methods for highlighting lines.Now I need to modify the render function to apply the highlight style. Let me look at the render function more closely.Now I need to modify the render function to apply highlighting. I'll need to:
1. Check if the current line is highlighted
2. Use the highlight style for line numbers when the line is highlighted
3. Apply the highlight background to content segments when the line is highlighted

Let me update the render function:Now I need to update the `highlightLine` function to accept the `is_highlighted` parameter and pass it along:Now I need to update `highlightContent` and `renderWithIndentGuides` to accept and use the `is_highlighted` parameter:Now update `renderWithIndentGuides`:Now I need to add the new helper functions `defaultSegmentHighlighted` and `styledSegmentHighlighted`. Let me find the existing helper functions:Now let me add the new helper functions after the existing ones:I need to check if Style has a `combineBackground` method. Let me check the Style module:Let me check what `combine` does:I see that `combine` exists and uses `other.bgcolor orelse self.bgcolor`. I need to take a different approach - I want to override just the background without affecting other properties. Let me use a simpler approach - just set the background directly:Now I need to update the `highlightZig`, `highlightJson`, and `highlightMarkdown` functions to accept and use the `is_highlighted` parameter. Let me find and update them:The `highlightZig` function is now accepting `is_highlighted` but not using it. I need to update all the `self.styledSegment` and `self.defaultSegment` calls to use the highlighted variants. Let me do that:Now let me find and update `highlightJson`:Now let me update `highlightMarkdown`:Now let me add unit tests for line highlighting. First let me see the end of the file where tests are:Now let me add the tests for line highlighting at the end of the file:Now let me run the tests to validate the implementation:All tests pass. Let me also run a quick sanity check by looking at the implementation to make sure everything is wired up correctly:All 636 tests pass. Let me update the todo list:Now let me commit the changes:The implementation is complete. Here's a summary of what was added:

**Feature: Highlight Specific Line Numbers**

Added the ability to highlight specific line numbers in the syntax highlighter, useful for showing error locations, breakpoints, or other marked lines.

**Changes:**

1. **SyntaxTheme** (`src/renderables/syntax.zig:348-411`):
   - Added `highlight_line_style` - background color for highlighted lines
   - Added `highlight_line_number_style` - style for line numbers on highlighted lines
   - Defined appropriate colors for default, monokai, and dracula themes

2. **Syntax struct** (`src/renderables/syntax.zig:568-587`):
   - Added `highlight_lines: ?[]const usize` field to store which lines to highlight

3. **Builder method** (`src/renderables/syntax.zig:690-696`):
   - Added `withHighlightLines(lines: []const usize)` for fluent API

4. **Rendering logic** (`src/renderables/syntax.zig:711-767`):
   - Updated render function to detect highlighted lines
   - Line numbers use bold yellow style when highlighted
   - Content segments receive highlight background color

**Usage example:**
```zig
const lines_to_highlight = [_]usize{ 5, 10, 15 };
const syntax = Syntax.init(allocator, code)
    .withLanguage(.zig)
    .withLineNumbers()
    .withHighlightLines(&lines_to_highlight);
```