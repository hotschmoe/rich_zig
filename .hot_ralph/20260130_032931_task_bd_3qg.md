I'll implement input prompts for the console. Let me start by exploring the current console implementation to understand the existing patterns.Now I understand the console structure. I'll add `input()` and `prompt()` methods for user input. Let me also check the Style module to understand styled prompts better.Now let me add the input functionality to the console. I'll add it after the existing methods, before the tests.Now let me add tests for the input functionality. I'll add them after the existing tests.Now I'll add tests for the input functionality at the end of the file.Tests pass. Now let me also run the build to make sure everything compiles correctly.Now let me commit the changes.Task completed successfully.

## Summary

Added console input prompt functionality to `src/console.zig`:

**New Types:**
- `InputOptions` - Configuration struct with: `default`, `password`, `show_default`, `validator`, `max_length`
- `InputValidationResult` - Union for validation results (`.valid` or `.invalid` with message)
- `InputError` - Error set for input operations

**New Methods:**
- `input(prompt_text)` - Simple text prompt
- `inputWithOptions(prompt_text, options)` - Full-featured input with options
- `prompt(prompt_markup)` - Styled prompt using markup syntax (e.g., `[bold red]Enter name[/]`)
- `promptWithOptions(prompt_markup, options)` - Styled prompt with full options

**Features:**
- Styled prompts via markup syntax
- Default values (shown in brackets, used when input is empty)
- Basic validation via function pointer
- Max length enforcement
- Windows-compatible line ending handling (strips `\r`)